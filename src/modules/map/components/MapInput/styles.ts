import {Colors, Constants} from '../../../../configs';

export const styles = {
  MapSearch: {
    position: 'absolute',
    left: 20,
    right: 20,
    top: Constants.statusbarHeight + 30,
    zIndex: 10,
    marginTop: -20,
  },

  MapSearchIcon: {
    position: 'absolute',
    top: 35,
    left: 13,
    zIndex: 11,
  },
};
